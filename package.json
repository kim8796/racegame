{
  "name": "racegame",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "lint": "node --check server/index.js && node --check server/game.js && node --check client/main.js && node --check tests/game.test.js && node --check eslint.config.js && if test -x node_modules/.bin/eslint; then eslint .; else echo 'eslint not installed; syntax checks passed'; fi",
    "test": "node --test",
    "build": "node --check server/index.js && node --check server/game.js && node --check client/main.js && node --check tests/game.test.js && node --check eslint.config.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "eslint": "^9.14.0"
  }
}
